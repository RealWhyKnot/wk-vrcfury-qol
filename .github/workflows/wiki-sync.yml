name: Sync wiki

# Mirrors wiki/ into the GitHub Wiki repo (RealWhyKnot/wk-vrcfury-qol.wiki.git).
# The wiki repo only materialises after a maintainer creates the first page via
# the web UI; before that, the clone step below fails fast and skips the sync.
# See CONTRIBUTING.md for the one-time bootstrap.

on:
  push:
    branches: [main]
    paths:
      - 'wiki/**'
      - '.github/workflows/wiki-sync.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: wiki-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Sync to wiki
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          REPO_URL="https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.wiki.git"

          # Clone the GitHub Wiki repo into a separate directory (wiki_remote/) so it
          # doesn't collide with the source-of-truth wiki/ folder already on disk.
          if ! git clone --depth 1 "$REPO_URL" wiki_remote 2>clone.err; then
            if grep -qi "repository not found" clone.err; then
              echo "::warning::Wiki repo doesn't exist yet -- visit the wiki on github.com and create the first page, then re-run this workflow."
              cat clone.err
              exit 0
            fi
            echo "::error::Failed to clone wiki repo"
            cat clone.err
            exit 1
          fi

          git -C wiki_remote config user.name  "github-actions[bot]"
          git -C wiki_remote config user.email "github-actions[bot]@users.noreply.github.com"

          # Mirror wiki/ into the wiki working tree.
          # Excludes: README.md (source-of-truth doc, not a wiki page); .git directory.
          rsync -a --delete \
            --exclude='.git' \
            --exclude='README.md' \
            wiki/ wiki_remote/

          if [ -z "$(git -C wiki_remote status --porcelain)" ]; then
            echo "No wiki changes to sync."
            exit 0
          fi

          git -C wiki_remote add -A
          git -C wiki_remote commit -m "Sync wiki from wiki/ @ ${GITHUB_SHA::7}

          Source: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/commit/${GITHUB_SHA}"
          git -C wiki_remote push origin HEAD
          echo "Wiki synced."
